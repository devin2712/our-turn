{
  "name": "our-turn",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^2.9.0"
  },
  "engines": {
    "node": "10"
  }
}
